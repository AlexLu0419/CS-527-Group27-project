You are optimizing a meta-prompt that generates code verification checkers.

The meta-prompt is used to generate Python checker functions that verify whether
a code patch correctly fixes a GitHub issue. The checkers are run deterministically
(no LLM at execution time), so they must be precise and robust.

## Current Meta-Prompt

```
{current_meta_prompt}
```

## Performance on Evaluation Set

Fitness score: {fitness_score:.3f}
True positives (correctly accepted good patches):  {true_positives}
True negatives (correctly rejected bad patches):   {true_negatives}
False positives (missed bad patches):              {false_positives}
False negatives (wrongly rejected good patches):   {false_negatives}

## Failure Case Analysis

Below are cases where the generated checkers made incorrect predictions.
For each case, you see the issue summary, what the checkers predicted, and
what the ground truth was.

{failure_analysis}

## Your Task

Propose 3 revised versions of the meta-prompt. Each should be a complete
replacement (not a diff). Each revision should specifically address one or
more of the failure patterns above.

Focus your revisions on:
- Making checker instructions more specific to avoid the failure patterns seen
- Adding or modifying guidance about what to check / what NOT to check
- Improving the output format instructions to get more robust checkers
- Adding examples or anti-examples based on observed failures

Common failure patterns to watch for:
- Checkers that are too strict (reject correct patches that use alternative approaches)
- Checkers that are too shallow (check file names but not actual code changes)
- Checkers that crash on edge cases (missing files, empty functions)
- Checkers that test for one specific implementation rather than the desired behavior

## Output Format

Output exactly 3 revised meta-prompts, separated by the delimiter:
===VARIANT_SEPARATOR===

Each variant should be a complete, standalone meta-prompt (not a diff).
Include a one-line comment at the top explaining what this variant changes:
# VARIANT: <brief description of what changed>
<full meta-prompt text>
