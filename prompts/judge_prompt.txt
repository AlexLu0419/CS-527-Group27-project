You are evaluating whether a code patch correctly resolves a GitHub issue.

You will see:
1. The original issue description
2. The proposed patch (git diff)
3. Results from automated structural checks (Layer 1) and generated verification
   checkers (Layer 2)

Your job is to make a final accept/reject decision for cases where the automated
checks were inconclusive.

## Issue Description

{issue_description}

## Proposed Patch (git diff)

```diff
{patch_diff}
```

## Automated Analysis Results

### Layer 1 — Structural Checks
Result: PASSED (all structural checks passed)

### Layer 2 — Instance-Specific Checkers
{layer2_details}

## Your Assessment

Analyze the patch against the issue and the automated results. Consider:

1. **Correctness**: Does the patch address the root cause described in the issue,
   or does it just suppress symptoms?
2. **Completeness**: Does the patch handle all cases mentioned in the issue, or
   does it only fix part of the problem?
3. **Regression risk**: Could this patch break existing functionality? Look for
   removed code, changed signatures, or altered control flow.
4. **Scope**: Is the patch appropriately scoped, or does it make unrelated changes
   that could introduce issues?

Important: When uncertain, lean toward ACCEPTING. A false rejection wastes a retry
attempt and may not lead to a better patch. Only reject when you have HIGH confidence
that the patch is wrong.

Respond in this exact JSON format (no markdown fences, no other text):

{
    "verdict": "accept" | "reject",
    "confidence": "high" | "medium" | "low",
    "reasoning": "2-3 sentence explanation of your assessment",
    "feedback_if_rejected": "Specific, actionable feedback for the developer to fix the patch. Empty string if accepting."
}
